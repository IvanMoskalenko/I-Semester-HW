namespace HW6

module Main =
    open System
    [<EntryPoint>]
    let main (argv: string array) =
        printfn "Enter input path: "
        let inputPath = Console.ReadLine()
        printfn "Enter output path: "
        let outputPath = Console.ReadLine()
        0

